// src/app/metadata.js
// Define the viewport configuration
export const viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
};

// Define the metadata configuration
export const metadata = {
  title: 'EV Range Prediction',
  description: 'EV Range Prediction Application'
};